spring:
  application:
    name: jifelog-platform
  config:
    import:
      - "optional:classpath:domain-application.yaml"

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health