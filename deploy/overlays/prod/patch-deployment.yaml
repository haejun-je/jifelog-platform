apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-api
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: platform-api
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          volumeMounts:
            - mountPath: /etc/secrets
              name: secret
              readOnly: true
          resources:
            requests:
              cpu: "500m"
              memory: "256Mi"
            limits:
              memory: "512Mi"
      volumes:
        - name: secret
          secret:
            secretName: jifelog-secret
